<!-- ============================================================================
touchstone/templates/review_report.md —— 评审报告版面模板（v2，2026-08 评审模板重设计）
--------------------------------------------------------------------------------
本文件是评审报告的固定「八股文」版面，是本系统的一等设计资产：
  - 版面（什么内容出现在哪一段、以什么顺序）由本文件唯一定义；
  - 代码（render.render_report）只做占位符填充，不得自行增删段落或调换顺序；
  - 版面变更 = 设计变更：须同步修订主设计文档「评审报告版面模板」章节并记变更历史。
六段版面（v2：七段 → 六段，去冗余；v3 瘦身：溯源横幅压一行、参考信息壳与「验证与日志」
折叠块移除——运行日志经 check-run 页可达，降级原始错误并入②告警区折叠块）：
  ① 标题 + 状态行（循环决策 + 轮次 + 销项率 + 风险等级，一行 blockquote）
  ② 告警（降级/CAUTION/溯源/同源提示，blockquote；空时省略；降级原始错误折叠块附于其后）
  ③ 静态检查（敏感路径/门禁；修改范围与 PR UI 重复→删；简单 PR 整段省略）
  ④ 评审发现与销项（AI 评审 + 清单合一；- [ ] task list 保留 checkbox；sig 兼位置+锚点）
  ⑤ 申报指引（「如何申报销项」<details>，无段标题壳，默认不占屏）
  ⑥ 机器 marker（loop + checklist + result，机读状态永远存在）
排版铁律（v2 确立；承袭易读性改版）：
  - 标题唯一：H2 只有报告标题一处；③④⑤ 一律 H3——并列段落必须并列层级；
  - 状态行（①）一律 blockquote——循环+风险合成一行（观测意见 6），替代旧版两行；
  - 告警（②）一律 blockquote；不可信时 [!CAUTION] 置顶、与其它告警以空行隔成各自块；
  - 发现（④）用 GitHub task list（- [ ]/- [x]，原生 checkbox）：每条 = 方向 + 状态 emoji
    + sig（兼位置与 ack 锚点）+ 嵌套详情（依据 <details>、达成判据、元数据小字）；
  - 去恒定噪声：修改范围删除（PR UI 已有）；敏感路径命中仅在有命中时显示；触发因子
    为空时省略；无敏感路径/门禁时整段③省略（简单 PR 零噪声）；
  - 评审不可信（review_reliable=False）必须以 [!CAUTION] 告警置顶（②），且不采信由
    "0 发现"推得的建议动作——0 发现 ≠ 审过没问题（PR #44 教训）。
占位符为空时该段整体省略（marker 段除外——机读状态永远存在）。
============================================================================ -->
## Touchstone · AI Committer 代码检视

{{status}}
{{alerts}}

{{facts}}

{{findings}}

{{reference}}

{{markers}}
